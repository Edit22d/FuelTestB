meta {
  name: 01 Register (Customer)
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/auth/register/
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "full_name": "Nabwire Edith",
    "email": "nabwireedith8@gmail.com",
    "phone_number": "+256744692055",
    "password": "Consistence@24",
    "confirm_password": "Consistence@24",
    "user_type": "customer",
    "location": "Kampala",
    "referral_code": ""
  }
}

script:post-response {
  if (res.status === 201 && res.body.tokens) {
    bru.setEnvVar("access_token", res.body.tokens.access);
    bru.setEnvVar("refresh_token", res.body.tokens.refresh);
  }
}
